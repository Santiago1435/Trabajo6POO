// La clase Main es la clase principal que contiene el método main, que es el punto de entrada del programa.
public class Main {
    // El método main es el punto de entrada principal del programa.
    public static void main(String[] args) {
        // Se crea una instancia de la clase Interfaz.
        Interfaz interfaz = new Interfaz();

        // Se hace visible la interfaz gráfica al establecer su visibilidad en true.
        interfaz.setVisible(true);
    }
}
